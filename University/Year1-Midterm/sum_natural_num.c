#include <stdio.h>
int main() {

    double n = 50;
    double sum_of_natural_num;

    sum_of_natural_num = n * (n + 1) / 2;
    printf("The result of added 1.5 numbers to so on is: %.2lf\n", sum_of_natural_num);

    return 0;
}